# pins.env
# Each line: NAME|URL|SHA256
# Put third-party tarballs/zip here. fetch_components.sh will download + verify.
# Example entries (REPLACE with real URLs + sha256):
# BOX64_TGZ|https://example.com/box64-wow64-linux-aarch64-2025-11-07.tar.zst|<sha256sum>
# WINE_WOW64_TGZ|https://example.com/wine-wow64-9.x-minimal.tar.zst|<sha256sum>
# DXVK_ZIP|https://example.com/dxvk-2.4.tar.xz|<sha256sum>
# FAudio_ZIP|https://example.com/faudio-25.01.zip|<sha256sum>

# (empty for now; overlay-only runtime is supported)
