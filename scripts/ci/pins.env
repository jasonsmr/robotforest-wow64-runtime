# ~/android/robotforest-wow64-runtime/scripts/ci/pins.env
# Canonical version pins used by CI and packer.
# Update these in a PR; never inline in workflows.

# Proton-GE (proton-ge-custom)
PROTON_TAG="GE-Proton10-9"

# DXVK (do NOT use 'master'; stable tags only)
DXVK_TAG="v2.4"

# vkd3d-proton
VKD3D_TAG="v2.13"

# Optional: known-good Box64 tag (if you vendor anything later)
# BOX64_TAG="v0.3.3"

# Runtime tag fallback if not provided by dispatcher
DEFAULT_TAG="dev"
