name: RF Runtime Release (sanity)

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Optional tag to echo'
        required: false
        default: ''

jobs:
  sanity:
    runs-on: ubuntu-latest
    steps:
      - name: Echo dispatch context
        run: |
          echo "::notice::EVENT=${{ github.event_name }}"
          echo "::notice::REF_NAME=${{ github.ref_name }}"
          echo "::notice::INPUT_TAG=${{ inputs.tag }}"
