# ---- RobotForest runtime repo ignores (canonical) ----

# outputs / payloads
/dist/
out/
*.log

# staging outputs we shouldn't track
staging/build/
staging/downloads/
staging/**/prefix/drive_c/users/*/Temp/
staging/**/prefix/drive_c/ProgramData/Package Cache/
staging/**/prefix/dosdevices/c:/windows/Temp/
staging/**/prefix/*.log

# GH Actions artifacts
**/*.artifact
**/*.artifact.zip

# editor/IDE noise
.*.swp
.idea/
.gradle/

# archives produced outside CI (safety net — prefer Releases artifacts)
*.zip
*.tar
*.tar.gz
*.tar.zst
_rf_latest_tmp/
